---
name: python-run-tests
description: Run executable verification on a Python/PySide6 app generated by this framework — venv, install, ruff, mypy, pytest, smoke launch. Use when the user asks to verify, check, validate, or test the project. Does not create a test suite if the project has none.
model: sonnet
---

# /python-run-tests — Executable verification

## Role
QA engineer — prove the project is clean and runnable with real commands.

## Goal
Run the verification ladder, surface any failure with its output, and confirm a clean state or hand off failures to `/python-fix-issue`.

## Deliverable
A short report in the user's language stating what passed, with command output as proof; or a list of failures.

---

> If the project root has not been provided in this flow, first ask: `Project root? (folder path)`.

## Steps

1. **Apply `@rules/verification.md §A`** in order:
   ```
   python -m venv .venv ; .venv\Scripts\activate
   pip install -r requirements.txt        # + requirements-dev.txt if present
   ruff check .                           # lint — clean
   ruff format --check .                  # format — clean
   mypy .                                 # type-check (strict) — clean
   pytest                                 # ONLY if tests/ exists — exit code 0
   python main.py                         # smoke launch (manual — no startup crash)
   ```
   - If the Salesforce CLI integration is on, the static read-through (`@rules/verification.md §B`) confirms every `sf` call goes through `models/sf_cli.py` via `subprocess.run` (args list, no `shell=True`). Tests never spawn the real `sf` — `subprocess.run` is mocked (`@rules/tests.md`).
2. **Capture output**. A non-zero exit or any reported error is a failure — quote the relevant lines.
3. **Report** (in the user's language): which commands passed, with proof. On failure: name the command, the `file:line`, and hand off to `/python-fix-issue` for the root cause (do not patch here unless the user asks).
4. If the **Python/venv environment is not available**: say so plainly, do the static read-through (`@rules/verification.md §B`), and list the exact commands the user must run themselves. Never claim a clean type-check you could not execute.

## Test creation policy

- **Do not create a test suite if the project has none** (Phase 1 tests = No). The default verification is ruff + mypy + smoke launch.
- If the user **explicitly asks** to add tests: follow `@rules/tests.md` (pytest + pytest-qt, source-mirroring structure, mocked-controller pattern, qtbot smoke views). Confirm scope, then add `requirements-dev.txt` if missing.

## Anti-patterns — what NOT to do
- **Do not** report "all green" without having actually run the commands (or without flagging that the env was unavailable).
- **Do not** fix code silently inside this skill — verification reports; fixing is `/python-fix-issue` (root cause) or `/python-add-feature`.
- **Do not** scaffold tests the user did not ask for.
- **Do not** lower a check (skip mypy, disable a ruff rule, `assert True`) to make the report pass.

## When the user asks something adjacent
- **"Make the tests pass"** → if a test fails, the root cause is in code or the test; classify with `/python-fix-issue`. Do not edit a test to assert the wrong behavior.
- **"Add coverage"** → explicit test creation; confirm scope, then write tests per `@rules/tests.md`.
